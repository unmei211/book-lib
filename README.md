# 📚 Система управления библиотекой

**Library Management System** — веб-приложение на Spring Boot для учёта книг, клиентов и бронирований в библиотеке.  
Позволяет добавлять, просматривать, редактировать книги и клиентов, а также создавать бронирования.

---

## 🚀 Быстрый запуск

### Требования
- JDK 8
- Maven
- PostgreSQL 14

### 1. Клонируйте репозиторий
```bash
git clone <ваш-репозиторий>
cd <репозиторий>
```

### 2. Скомпилируйте
```bash
mvn package
```

### 3. Запустите, передав переменные окружения
Ознакомьтесь с .env.template

```bash
POSTGRES_HOST=localhost \
POSTGRES_PORT=5432 \
POSTGRES_DB=postgres \
POSTGRES_USER=postgres \
POSTGRES_PASSWORD=postgres \
LOG_LEVEL=DEBUG \
ENV=local \
java -jar target/*.jar
```

### 4. Использование приложения
Основная страница доступна по адресу localhost:8080/

REST запрос на получение деталей броней доступен по адресу localhost:8080/bookings/details и описан в api/api.yaml в корне проекта